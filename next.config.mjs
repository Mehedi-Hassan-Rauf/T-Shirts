/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: false,
  images: {
    // protocol: "https",
    domains: ["files.stripe.com"],
  },
};

export default nextConfig;
